{% extends '::base.html.twig' %}

{% block body -%}
    <h1>CallResult list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Outerid</th>
                <th>Name</th>
                <th>Isaactive</th>
                <th>Createdat</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('call_result_manage_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.outerId }}</td>
                <td>{{ entity.name }}</td>
                <td>{{ entity.isAActive }}</td>
                <td>{% if entity.createdAt %}{{ entity.createdAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('call_result_manage_show', { 'id': entity.id }) }}">show</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    {% endblock %}
