{% extends '::base.html.twig' %}

{% block body -%}
    <h1>CallResult</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Outerid</th>
                <td>{{ entity.outerId }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ entity.name }}</td>
            </tr>
            <tr>
                <th>Isaactive</th>
                <td>{{ entity.isAActive }}</td>
            </tr>
            <tr>
                <th>Createdat</th>
                <td>{{ entity.createdAt|date('Y-m-d H:i:s') }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('call_result_manage') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
