{% extends '::base.html.twig' %}

{% block body -%}
    <h1>BranchPhones list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Phone</th>
                <th>Isactive</th>
                <th>Createdat</th>
                <th>Updatedat</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('branches_manage_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.phone }}</td>
                <td>{{ entity.isActive }}</td>
                <td>{% if entity.createdAt %}{{ entity.createdAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if entity.updatedAt %}{{ entity.updatedAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('branches_manage_show', { 'id': entity.id }) }}">show</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    {% endblock %}
